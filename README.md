# Automated Resume Customizer

MVP resume ATS optimizer for tailoring Google Docs resume text to a job description while preserving authenticity.

## Setup

Install dependencies and run:

```bash
npm install
npm run dev
```

No paid AI API key is required. The MVP uses local rule-based scoring and conservative rewrite suggestions.
